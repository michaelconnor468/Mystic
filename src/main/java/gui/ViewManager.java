package gui;

/**
 * Made to act as a "docking" interface between views that will be in use allowing only the currently in use one to dock. 
 * This allows for easy swapping between views such as start menu, gmae, inventory, e.x.
 */
public class ViewManager {

}
